class Boss extends Enemy {
  constructor(x, y, size, img, hp, dmg, speed, path, reward) {
    super(x, y, size, img, hp, dmg, speed, path, reward);
  }
  update(){
    super.update();
  }
  
  draw(){
    super.draw();
  }
}